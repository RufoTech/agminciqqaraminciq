{
  "name": "brendex-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "start": "npm --prefix backend start",
    "build": "npm --prefix backend install"
  }
}
